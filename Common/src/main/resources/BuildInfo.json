{
  "name": "CustomSkinLoader",
  "version": "${modVersion}",
  "fullVersion": "${modFullVersion}",
  "dependencies": {
    "${dependencies}"
  },
  "gitVersion": "${gitVersion}",
  "buildUrl": "${buildUrl}",
  "buildTime": "${releaseTime}"
}
