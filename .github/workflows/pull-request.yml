name: Pull Request

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build-pull-request:
    name: Build Pull Request
    uses: ./.github/workflows/common.yml
    with:
      type: PullRequest
      publish: false
      release: false
      is-snapshot: true
