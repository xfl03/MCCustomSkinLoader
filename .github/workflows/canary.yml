name: Canary
on:
  push:
    branches:
      - '15-develop'

jobs:
  build-canary:
    name: Build Canary
    uses: xfl03/MCCustomSkinLoader/.github/workflows/common.yml@15-develop
    with:
      type: Canary
      publish-task: uploadCanary
    secrets: inherit