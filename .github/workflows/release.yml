name: Release
on:
  push:
    tags:
      - '*'

jobs:
  build-release:
    name: Build Release
    uses: ./.github/worlflows/common.yml
    with:
      type: Release
      publish-task: upload
      is-snapshot: false
    secrets: inherit