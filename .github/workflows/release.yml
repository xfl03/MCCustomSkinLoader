name: Release

on:
  release:
    types: [released]

jobs:
  build-release:
    name: Build Release
    uses: ./.github/workflows/common.yml
    with:
      type: Release
      release: true
      latest-json: latest.json
      detail-json: detail.json
      is-snapshot: false
    secrets: inherit
